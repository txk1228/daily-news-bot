# 每日科技新闻推送机器人 - 依赖包

# 定时任务调度
APScheduler>=3.11.2

# HTTP 请求
requests==2.31.0

# Coze 开发工具包（包含搜索功能）
coze-coding-dev-sdk>=0.5.0,<1

# 运行时上下文工具
coze-coding-utils>=0.2.7,<1

# Coze SDK 传递依赖缺失，显式声明（SDK 运行时需要但不声明）
beautifulsoup4
python-pptx
openpyxl
pypdf
docx2python
